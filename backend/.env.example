# ============================================================================
# Configurações de Ambiente - Películas Araujo Backend
# ============================================================================
# 
# Este arquivo serve como template para variáveis de ambiente.
# Copie para .env e preencha com seus valores reais.
#
# NUNCA commite o arquivo .env real no repositório por questões de segurança!
# ============================================================================

# Modo de execução
# Valores: 'development' ou 'production'
# Em development: debug=True, reload automático
# Em production: debug=False, mais seguro
FLASK_ENV=development

# Chave secreta para sessões e tokens
# Altere este valor em produção com uma chave aleatória e segura
# Gerar: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=dev-key-change-in-production

# Porta na qual o servidor irá executar
# Padrão: 5000
PORT=5000

# Host para executar o servidor
# 0.0.0.0: Acessível de qualquer IP
# localhost ou 127.0.0.1: Apenas local
HOST=0.0.0.0

# ============================================================================
# Configurações do Banco de Dados (para implementação futura)
# ============================================================================

# DATABASE_URL=sqlite:///app.db
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=usuario
# DATABASE_PASSWORD=senha
# DATABASE_NAME=peliculas_db

# ============================================================================
# Configurações de Email (para implementação futura)
# ============================================================================

# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# EMAIL_USER=seu-email@gmail.com
# EMAIL_PASSWORD=sua-senha-de-app
# EMAIL_FROM=noreply@peliculasaraujo.com

# ============================================================================
# Configurações de CORS
# ============================================================================

# URLs permitidas para requisições CORS
# Em desenvolvimento, pode ser * (qualquer origem)
# Em produção, especifique os domínios permitidos
CORS_ORIGINS=*

# ============================================================================
# Configurações de Log
# ============================================================================

# Nível de log
# Valores: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Arquivo de log
# LOG_FILE=logs/app.log
